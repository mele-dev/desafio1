'use strict'

console.log('hola');

const numero = 4;

const factorial = () => {
    if (num < 0) {
        throw new Error('El numero es menor a 0');
    }
    else if (num <= 1) {
        return 1;
    }
}